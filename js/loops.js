//================ loops =================

//============= FOR ============
// diffetent ways to increment variables
// 1. x = x + 1;
//2. x += 1;
//3. x++;

for (var x = 1; x < 10; x++){
  console.log(x);
}

//============= while ===============

var y = 7;
while ( y > 0){
console.log(y);
// The conditional must at one point stop so there must be something that happens to the variable each time
y--;
}
